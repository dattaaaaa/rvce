// client/src/components/professor/EnrolledStudentsList.jsx
import React from 'react';
import Card from '../common/Card';

const EnrolledStudentsList = () => (
    <Card title="Enrolled Students (Course: AI233AI)">
        <p>A list of all students enrolled in the professor's assigned course.</p>
    </Card>
);
export default EnrolledStudentsList;