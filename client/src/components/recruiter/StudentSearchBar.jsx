// client/src/components/recruiter/StudentSearchBar.jsx
import React from 'react';
import Card from '../common/Card';

const StudentSearchBar = () => (
    <Card title="Find Candidates">
        <p>A search bar with filters (branch, CGPA, skills) for recruiters to find students.</p>
    </Card>
);
export default StudentSearchBar;